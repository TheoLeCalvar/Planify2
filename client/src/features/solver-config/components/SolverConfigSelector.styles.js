const styles = {
  solverConfigSelector: {
    p: 1,
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 1,
  },
  configSelect: {
    minWidth: 300, // Using Material-UI's spacing, this sets a minimum width of 300px.
  },
};

export default styles;
